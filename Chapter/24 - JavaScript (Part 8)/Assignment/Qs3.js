// Qs3. Create a new array whose elements are in uppercase of words present in the
//      original array

let Marvel = ["Iron Man", "Spider-Man", "Captain America", "Dr. Strange"];

let inUppercase = Marvel.map((word) => word.toUpperCase());

console.log(inUppercase);
